

document.querySelector("form").addEventListener("submit",goto)

var cred=JSON.parse(localStorage.getItem(signupdetails));

function goto(event){
    event.preventDefault();

    // var email = document.


    // if(user)
    window.location.href="index.html";
}

